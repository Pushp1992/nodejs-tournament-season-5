// all routes HTTP method (GET/PUT/POST/DELETE) will be present here

module.exports = (app) => {
    // import your existing Health controller here, which contains method for all CRUD operations

    // APi endpoint to create a new Health Center

    // API endpoint to get all Health Center

    // API endpoint to get single Health Center

    // API endpoint to update Health Center with centerId

    // API endpoint to delete Health Center with centerId

};