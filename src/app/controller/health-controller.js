// All controller logic for CRUD operation will stay here

const HealthModel = require('../model/health-model');

// Create and Save a new health Center
exports.create = (req, res) => {
    
    // validate request body

    // Creaate health Center

    // Save health Center in Database
 
};


// Retrieve and return all health Center from the database.
exports.findAll = (req, res) => {
   // TODO from below
};

// Find a single health Center with a centerId
exports.findOne = (req, res) => {
    // TODO from below
};

// Update a Health Center identified by the centerId in the request
exports.update = (req, res) => {
    // TODO from below
};

// Delete a Health Center with the specified centerId in the request
exports.delete = (req, res) => {
    // TODO from below
};